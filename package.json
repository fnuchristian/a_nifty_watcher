{
  "name": "a_nifty_watcher",
  "version": "0.0.0",
  "description": "an html file watcher that updates real time via http protocol",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christian",
  "license": "ISC",
  "dependencies": {
    "express": "^4.6.1",
    "morgan": "^1.2.0"
  }
}
